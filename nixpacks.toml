# nixpacks.toml
providers = ["...", "php", "node"]

[variables]
    NIXPACKS_PHP_ROOT_DIR = "/app/public"

[phases.postbuild]
    cmds = [
        "php artisan optimize"
    ]
    dependsOn = ["build"]
